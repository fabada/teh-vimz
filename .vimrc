" Syntax highlighting
syntax on

" Highlight searches
set hlsearch

set nu

" Case insensitive search, except when search has capital letters
set ignorecase
set smartcase

" tabs = 4 spaces
set expandtab
set tabstop=4
